# This rule is not used by the project dc-controller itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to dataconnecthub.opendatahub.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: dc-controller
    app.kubernetes.io/managed-by: kustomize
  name: dataconnecthub-viewer-role
rules:
- apiGroups:
  - dataconnecthub.opendatahub.io
  resources:
  - dataconnectservices
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - dataconnecthub.opendatahub.io
  resources:
  - dataconnectservices/status
  verbs:
  - get
