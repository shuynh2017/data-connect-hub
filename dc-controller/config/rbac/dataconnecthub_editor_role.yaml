# This rule is not used by the project dc-controller itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the components.platform.opendatahub.io.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: dc-controller
    app.kubernetes.io/managed-by: kustomize
  name: dataconnecthub-editor-role
rules:
- apiGroups:
  - components.platform.opendatahub.io
  resources:
  - dataconnecthubs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - components.platform.opendatahub.io
  resources:
  - dataconnecthubs/status
  verbs:
  - get
