name: Build and Test

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v7

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.96.0"
          components: clippy, rustfmt

      - name: Cache cargo registry and build
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked

      - name: Build
        run: make build

      - name: Lint
        run: make lint

      - name: Unit tests
        run: make test-unit

      - name: Doc check
        run: make doc

      - name: Verify API documentation is up to date
        run: |
          make generate-openapi-docs
          git diff --exit-code -- docs/api/ || (echo "API documentation is out of date. Run 'make generate-openapi-docs' locally." && exit 1)

      - name: Security audit
        run: make audit
