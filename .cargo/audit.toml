[advisories]
# Marvin Attack (timing sidechannel) in the `rsa` crate — transitive dep via sqlx,
# no upstream fix available as of 2026-07.
# h2 0.3.x unbounded empty DATA frames — transitive dep via actix-http 3.x,
# awaiting actix-http upgrade to h2 0.4.x.
ignore = ["RUSTSEC-2023-0071", "RUSTSEC-2026-0258"]
