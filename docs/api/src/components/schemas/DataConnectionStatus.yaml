type: object
required: [state]
properties:
  state:
    $ref: "DataConnectionState.yaml"
  message:
    type: string
    nullable: true
    description: Optional human-readable status message
  phases:
    type: array
    items:
      $ref: "PhaseCondition.yaml"
    description: Detailed phase conditions for this connection
