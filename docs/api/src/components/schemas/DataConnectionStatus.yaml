type: object
required: [state]
properties:
  state:
    $ref: "DataConnectionState.yaml"
  message:
    type: string
    nullable: true
    description: Optional human-readable status message
  updated_at:
    type: string
    nullable: true
    description: Timestamp of the last status update
